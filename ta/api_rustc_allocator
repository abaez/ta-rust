AllocFnFactory			impl<'a> AllocFnFactory<'a> 
AllocFnFactory			struct AllocFnFactory<'a> 
AllocatorMethod			pub struct AllocatorMethod 
AllocatorTy			pub enum AllocatorTy 
ExpandAllocatorDirectives			struct ExpandAllocatorDirectives<'a> 
Folder for ExpandAllocatorDirectives			impl<'a> Folder for ExpandAllocatorDirectives<'a> 
alloc_err_ptr			    fn alloc_err_ptr(&self) -> P<Ty> 
allocator_fn			    fn allocator_fn(&self, method: &AllocatorMethod) -> P<Item> 
arg_ty			    fn arg_ty(&self,
attrs			    fn attrs(&self) -> Vec<Attribute> 
call_allocator			    fn call_allocator(&self, method: &str, mut args: Vec<P<Expr>>) -> P<Expr> 
expand			pub mod expand;
fold_item			    fn fold_item(&mut self, item: P<Item>) -> SmallVector<P<Item>> 
fold_mac			    fn fold_mac(&mut self, mac: Mac) -> Mac 
layout_ptr			    fn layout_ptr(&self) -> P<Ty> 
modify			pub fn modify(sess: &ParseSess,
ptr_u8			    fn ptr_u8(&self) -> P<Ty> 
ptr_usize			    fn ptr_usize(&self) -> P<Ty> 
ret_ty			    fn ret_ty(&self,
